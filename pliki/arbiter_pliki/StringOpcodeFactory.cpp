#include "StringOpcodeFactory.hpp"

namespace arbiter
{
/*
    std::string StringOpcodeFactory::createStringFromOpcode(const Opcode opcode)
    {
        //StringOpcodeFactory::callbacks_.at(0);
        if(!fabricWasCreated_)
            createFactory();
        auto iter = callbacks_.find(opcode);
        if(iter == callbacks_.end())
            return "";
        return iter->second;



    }
    void StringOpcodeFactory::createFactory()
    {
        StringOpcodeFactory::callbacks_.insert(std::pair<Opcode, std::string>(DAT, "DAT") );
        StringOpcodeFactory::callbacks_.insert(std::pair<Opcode, std::string>(MOV, "MOV") );
        StringOpcodeFactory::callbacks_.insert(std::pair<Opcode, std::string>(ADD, "ADD") );
        callbacks_.insert(std::pair<Opcode, std::string>(SUB, "SUB") );
        callbacks_.insert(std::pair<Opcode, std::string>(JMP, "JMP") );
        fabricWasCreated_ = true;
    }
*/
}

